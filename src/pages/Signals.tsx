import { StubPage } from "@/components/oracle/StubPage";

const Signals = () => (
  <StubPage
    title="Prediction market signals"
    description="Probability moves, whale activity, news catalysts, liquidity shifts, anomalies and upcoming resolutions — surfaced as actionable signals."
  />
);

export default Signals;