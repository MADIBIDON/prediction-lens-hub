import { StubPage } from "@/components/oracle/StubPage";

const Calendar = () => (
  <StubPage
    title="Resolution calendar"
    description="Every market resolution, central bank decision, election, court ruling and major event in one calendar — linked to the markets they will move."
  />
);

export default Calendar;