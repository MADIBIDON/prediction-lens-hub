import { StubPage } from "@/components/oracle/StubPage";

const News = () => (
  <StubPage
    title="News driving markets"
    description="A live news feed where every story is connected to the prediction markets it moves, with the resulting probability change."
  />
);

export default News;